module.exports = {
  lessBuild: function() {
    gulp         = global.gulp;
    gulpLess     = global.gulpLess;
    foldersSrc   = global.options.folders.src;
    foldersBuild = 

    gulp.src()
  }
}
